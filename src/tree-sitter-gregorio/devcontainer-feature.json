{
    "id": "tree-sitter-gregorio",
    "version": "1.0.4",
    "name": "Tree-Sitter Gregorio",
    "description": "Installs tree-sitter-gregorio grammar from source on Debian/Ubuntu, Red Hat/Fedora, or Alpine based systems.",
    "documentationURL": "https://github.com/AISCGre-BR/devcontainer-features",
    "licenseURL": "https://github.com/AISCGre-BR/devcontainer-features/blob/main/LICENSE",
    "keywords": [
        "tree-sitter",
        "gregorio",
        "parsing",
        "grammar"
    ],
    "options": {
        "host": {
            "type": "string",
            "default": "github",
            "description": "Repository host platform (e.g. 'github', 'gitlab', 'codeberg', 'bitbucket'). Used to construct the repository URL.",
            "proposals": [
                "github",
                "gitlab",
                "codeberg",
                "bitbucket"
            ]
        },
        "repository": {
            "type": "string",
            "default": "aiscgre-br/tree-sitter-gregorio",
            "description": "Repository in OWNER/REPO format (e.g. 'aiscgre-br/tree-sitter-gregorio'). Used together with host to construct the full repository URL."
        },
        "ref": {
            "type": "string",
            "default": "",
            "description": "Git reference (branch name, tag name, or commit hash) to build from. The value is passed as-is to the forge archive endpoint. Leave empty to build from the HEAD of the repository's default branch.",
            "proposals": [
                "",
                "latest",
                "v0.5.2",
                "main"
            ]
        }
    }
}
