{
    "scenarios": [
        {
            "id": "gregorio_lsp_skipped",
            "description": "No ref specified — install gregorio-lsp from the HEAD of the default branch",
            "options": {}
        },
        {
            "id": "gregorio_lsp_specific_tag",
            "description": "Build and install a specific gregorio-lsp version from source",
            "options": {
                "ref": "v0.9.4"
            }
        },
        {
            "id": "gregorio_lsp_latest",
            "description": "Build and install the latest gregorio-lsp version from source",
            "options": {
                "ref": "latest"
            }
        },
        {
            "id": "gregorio_lsp_with_custom_host",
            "description": "Install gregorio-lsp from a custom host (e.g. GitLab)",
            "options": {
                "ref": "latest",
                "host": "gitlab",
                "repository": "aiscgre-br/gregorio-lsp"
            }
        },
        {
            "id": "gregorio_lsp_with_branch",
            "description": "Install gregorio-lsp from a specific branch",
            "options": {
                "ref": "main"
            }
        }
    ]
}
